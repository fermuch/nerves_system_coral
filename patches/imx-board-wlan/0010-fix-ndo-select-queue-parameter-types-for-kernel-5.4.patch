From: AI Assistant <assistant@example.com>
Date: Tue, 30 Jul 2025 13:30:00 -0300
Subject: [PATCH] Fix ndo_select_queue parameter types for kernel 5.4

This patch fixes the actual parameter types for hdd_hostapd_select_queue function.
For kernel 5.4+, the third parameter should be 'struct net_device *sb_dev'
instead of 'void *accel_priv'.

diff --git a/CORE/HDD/src/wlan_hdd_wmm.c b/CORE/HDD/src/wlan_hdd_wmm.c
index 1234567..abcdefg 100644
--- a/CORE/HDD/src/wlan_hdd_wmm.c
+++ b/CORE/HDD/src/wlan_hdd_wmm.c
@@ -1839,8 +1839,12 @@
   ===========================================================================*/
 v_U16_t hdd_hostapd_select_queue(struct net_device * dev, struct sk_buff *skb
 #if (LINUX_VERSION_CODE >= KERNEL_VERSION(3,13,0))
+#if (LINUX_VERSION_CODE >= KERNEL_VERSION(5,4,0))
+                                 , struct net_device *sb_dev
+#else
                                  , void *accel_priv
 #endif
+#endif
 #if (LINUX_VERSION_CODE >= KERNEL_VERSION(3,14,0)) && (LINUX_VERSION_CODE < KERNEL_VERSION(5,4,0))
                                  , select_queue_fallback_t fallback
 #endif
---